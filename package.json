{
  "name": "jewellery-inventory",
  "version": "1.0.0",
  "description": "Inventory and GST billing app for jewellery shop",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "npm install && npm install --prefix client",
    "dev": "concurrently -n server,client -c green,cyan \"node server/index.js\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run build --prefix client && node server/index.js",
    "hash-password": "node scripts/hash-password.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
